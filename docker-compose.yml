services:
  nextjs:
    build:
      context: .
      args:
        NEXT_PUBLIC_ADMIN_API_BASE_URL: ${NEXT_PUBLIC_ADMIN_API_BASE_URL}

    image: impetrosys/rootui-app:v1_27May

    container_name: rootui-app

    ports:
      - "3001:3001"

    restart: always

    env_file:
      - .env